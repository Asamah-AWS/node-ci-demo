name: Node.js CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Cache Node.js modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npx eslint .

      - name: Run tests with coverage
        run: npm test -- --coverage --coverageProvider=v8

  staging:
    needs: test
    runs-on: ubuntu-latenst
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install dependencies
        run: npm install 
      - name: Deploy to staging 
        run: |
          echo "Simulating deployment to staging environment..."
        # This is where you'd put your real deploy commands later#Add your staging deploy commands here 
  
  canary:
    runs-on: ubuntu-latest
    needs: staging
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install dependencies
        run: npm install
      - name: Simulate Canary Deployment
        run: |
          echo "Simulating canary deployment..."
        # Replace this with your actual canary deploy commands later


  deploy:
    runs-on: ubuntu-latest
    needs: canary
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to production
        run: echo "Deploying to Production env..."
        # Add your production deploy commands here
